import { AlumfilterPipe } from './alumfilter.pipe';

describe('AlumfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlumfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
