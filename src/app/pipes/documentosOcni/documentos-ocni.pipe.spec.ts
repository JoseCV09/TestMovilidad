import { DocumentosOcniPipe } from './documentos-ocni.pipe';

describe('DocumentosOcniPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentosOcniPipe();
    expect(pipe).toBeTruthy();
  });
});
